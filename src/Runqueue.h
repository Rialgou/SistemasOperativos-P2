#ifdef _Runqueue_H_
#define _Runqueue_H_
#include <vector>
#include <stack>

struct RQ{
    vector<stack> rq;    
};


class Runqueue{
private:

public:
    Runqueue();
    ~Runqueue();
    
};

#endif